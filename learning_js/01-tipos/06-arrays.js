// para crear un array lo podemos hacer de la siguiente manera
let animales = ['perro', 'gato'];
console.log(animales);

// Ahora si queremos consultar el valor en una poscicion indicada lo
// hacemos de la siguiente manera
console.log(animales[0]);

//Ahora si queremos agregar datos a dicho array lo indicamos asi:
animales[2] = 'dragon'
console.log(animales)